name: CI
on:
  push

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: anndutova/go-course-tests:latest
    steps:
      - uses: actions/checkout@v4

      - name: Run test
        run: cd KthLargest
